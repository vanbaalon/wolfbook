# Node
node_modules/

# Intermediate build artifacts (keep wstp/build/Release/wstp.node)
wstp/build/Release/*.o
wstp/build/Release/*.a
wstp/build/Release/obj.target/

# VSIX packages (build artifacts)
*.vsix

# macOS
.DS_Store

# VS Code local settings
.vscode/

# Temporary large Docs/ folder (use lowercase docs/ for tracked documentation)
Docs/
!docs/

# Internal AI agent reference (lives in parent folder, not tracked here)
AGENTS.md

# Agent guidance lives at the workspace root and is Dropbox-synced only.
# Guard against a stray copy ever being committed to the public repo.
CLAUDE.md
AGENTS.md
